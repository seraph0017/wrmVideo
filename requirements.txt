# 视频生成项目依赖

# 核心依赖
requests==2.31.0
volcengine-python-sdk[ark]==1.0.98
volcengine==1.0.98
ffmpeg-python==0.2.0
jinja2>=3.0.0
Pillow>=10.0.0
moviepy>=1.0.3
numpy>=1.21.0

# Web应用
Django>=4.2.0
django-bootstrap5>=25.0.0
mysqlclient>=2.2.0

# 文本处理
jieba>=0.42.1

# 配置文件处理
PyYAML>=6.0
python-dotenv>=1.0.0

# 开发和测试
pytest>=7.0.0
pytest-asyncio>=0.21.0

# 异步处理
aiofiles>=23.0.0
# gevent>=23.0.0  # 已替换为标准库的concurrent.futures

# Redis和消息队列
redis>=4.5.0
celery>=5.3.0
django-celery-beat>=2.5.0
django-celery-results>=2.5.0

# WebSocket和实时通信
channels>=4.0.0
channels-redis>=4.1.0

# 路径处理
pathlib2>=2.3.7; python_version<'3.4'